using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CristinaGameManager : MiniGameManager
{
    // Start is called before the first frame update
    void Start()
    {
        score.current_scene = SCENES.CRIS;
    }
}
